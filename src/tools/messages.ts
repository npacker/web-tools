/**
 * User-facing message literals shared across multiple tools.
 */

/**
 * Warning emitted when an image-download batch yields no files.
 */
export const IMAGE_DOWNLOAD_EMPTY_BATCH_MESSAGE = "Error fetching images"
